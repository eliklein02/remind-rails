<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "InLoop" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=yes">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <link rel="icon" href="/inloop-logo.png" type="image/png">
    <link rel="apple-touch-icon" href="/inloop-logo.png">
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= action_cable_meta_tag %>
  </head>
  <body>
    <div class="min-h-screen flex flex-col">
      <%= render 'layouts/navbar' %>
      <% console %>
      <%= render 'layouts/notifications' %>
      <main class="flex-grow container mx-auto mt-28 px-5 flex flex-col">
        <%= yield %>
      </main>
      <%= render 'layouts/footer' %>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/flowbite@3.1.2/dist/flowbite.turbo.min.js"></script>
  </body>
</html>
